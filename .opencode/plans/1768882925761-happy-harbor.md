# Plan: Pod namespace guidance

## Phase 1: Initial understanding
- [x] Read `backend/danube/k8s/client.py` to see current namespace handling.
- [x] Identify whether other namespace helpers exist (none found yet).
- [x] Clarify intended source of namespace (config vs caller) with user.

## Phase 2: Design
- Provide guidance: keep `metadata.namespace` populated from the `create_pod` caller-provided `namespace` argument.
- Recommend callers use config defaults (`kubernetes.namespace_system` / `kubernetes.namespace_jobs`) when deciding which namespace to pass, but do not hardcode in `K8sClient`.
- Highlight that omitting namespace will default to the client context namespace and can create pods in the wrong namespace.
- Point to docs for configuration: `docs/configuration/server-config.md` and `docs/configuration/blueprint-reference.md`.

## Phase 3: Review
- Ensure guidance aligns with caller-provided namespace requirement and existing docs.

## Phase 4: Final plan (execution)
- Answer the user with the correct usage pattern, including a code snippet showing `create_pod(namespace=...)` and a note on sourcing the namespace from config.
- Mention gotchas: missing namespace defaults, system vs jobs split, and validation expectations in server config.

## Verification
- No code changes required; no tests to run.

